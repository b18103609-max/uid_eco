import{useState}from'react';import{filterSafety,summarizeSafety}from'../calculations/safety.ts';import{SAFETY_RECORDS}from'../data/safetyData.ts';import type{AnalyticsFilters}from'../filters.ts';import'./safety.css';
export const SafetyDashboard=({filters}:{filters:AnalyticsFilters})=>{const[segment,setSegment]=useState('Все');const rows=filterSafety(SAFETY_RECORDS,filters),s=summarizeSafety(rows),details=segment==='Все'?rows:rows.filter(x=>x.severity===segment||x.source===segment);const sources=['ВА','ВК','Прочие ПБ']as const,severities=['Критическое','Значительное','Незначительное']as const;if(!rows.length)return <section className="analytics-empty"><h2>По выбранным фильтрам нет нарушений</h2></section>;return <div className="safety-dashboard">
<section className="safety-kpis"><article><span>Нарушения рейтинга</span><strong>{s.total}</strong><small>ВА, ВК и прочие ПБ</small></article><article><span>Критические</span><strong>{s.critical}</strong><small>прямая тяжесть</small></article><article><span>Открытые</span><strong>{s.open}</strong><small>по сопоставленному статусу</small></article><article><span>Стоп-факторы</span><strong>{s.stopFactors}</strong><small>прямой признак рейтинга</small></article><article><span>Самооценка ниже порога</span><strong>{s.belowThreshold}</strong><small>бригад</small></article><article><span>Договорные без мероприятия</span><strong>{s.withoutMeasure}</strong><small>по физической связи</small></article></section>
<section className="safety-main"><article className="safety-card"><header><h2>Как нарушения распределены по источнику и тяжести?</h2><span>Совместное распределение без вывода о причинности</span></header><div className="safety-matrix">{sources.map(source=><div key={source}><strong>{source}</strong>{severities.map(severity=>{const count=rows.filter(x=>x.source===source&&x.severity===severity).length;return<button key={severity} onClick={()=>setSegment(severity)} className={`sev-${severity.toLowerCase()}`}><span>{severity}</span><b>{count}</b></button>})}</div>)}</div></article><aside className="safety-attention"><header><h2>Требует внимания</h2><span>Только прямые критические признаки</span></header>{rows.filter(x=>x.severity==='Критическое'||x.stopFactor||x.selfAssessment<x.threshold).slice(0,5).map(x=><button key={x.id} onClick={()=>setSegment(x.severity)}><strong>{x.id} · {x.brigade}</strong><span>{x.stopFactor?'Стоп-фактор':x.severity}</span><small>{x.contractor} · самооценка {x.selfAssessment}%</small></button>)}</aside></section>
<section className="safety-secondary"><article className="safety-card"><header><h2>Самооценка и минимальный порог</h2></header><div className="assessment-bars">{[...new Map(rows.map(x=>[x.brigade,x])).values()].map(x=><div key={x.brigade}><span>{x.brigade}</span><i><b style={{width:`${x.selfAssessment}%`}}/><em style={{left:`${x.threshold}%`}}/></i><strong>{x.selfAssessment}%</strong></div>)}</div></article><article className="safety-card"><header><h2>Договорные нарушения и мероприятия</h2><span>Связь подтверждена lnk_contract_measure_breach</span></header><div className="safety-links"><div><span>Договорные нарушения</span><strong>{s.contractBreaches}</strong></div><div><span>Со связанным мероприятием</span><strong>{s.contractBreaches-s.withoutMeasure}</strong></div><div><span>Без связанного мероприятия</span><strong>{s.withoutMeasure}</strong></div></div></article></section>
<section className="safety-details"><header><h2>Расшифровка: {segment.toLowerCase()}</h2>{segment!=='Все'&&<button onClick={()=>setSegment('Все')}>Сбросить сегмент</button>}</header><div>{details.slice(0,5).map(x=><article key={x.id}><strong>{x.id} · {x.brigade}</strong><span>{x.source} · {x.severity.toLowerCase()}</span><small>{x.status} · {x.contractor}</small></article>)}</div></section></div>};
