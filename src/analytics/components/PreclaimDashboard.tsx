import{filterPreclaims,summarizePreclaims}from'../calculations/preclaim.ts';import{PRECLAIM_CASES}from'../data/preclaimData.ts';import type{AnalyticsFilters}from'../filters.ts';import'./preclaim.css';const fmt=(n:number)=>new Intl.NumberFormat('ru-RU',{maximumFractionDigits:1}).format(n);
export const PreclaimDashboard=({filters}:{filters:AnalyticsFilters})=>{const rows=filterPreclaims(PRECLAIM_CASES,filters),s=summarizePreclaims(rows),stages=['Требование','Претензия','Суд','Закрыто']as const;if(!rows.length)return<section className="analytics-empty"><h2>По выбранным фильтрам нет случаев ДПР</h2></section>;return<div className="preclaim-dashboard"><section className="preclaim-kpis"><article><span>Случаи ДПР</span><strong>{s.total}</strong><small>в выборке</small></article><article><span>Сумма требований</span><strong>{fmt(s.amount)}</strong><small>млн ₽</small></article><article><span>С отклонением</span><strong>{s.deviated}</strong><small>готовая колонка формы</small></article><article><span>Признано</span><strong>{fmt(s.recognized)}</strong><small>млн ₽</small></article><article><span>Отклонено / отозвано</span><strong>{fmt(s.rejected)}</strong><small>млн ₽</small></article><article><span>Передано в суд</span><strong>{s.court}</strong><small>случаев</small></article></section><section className="preclaim-main"><article className="preclaim-card"><header><h2>На каких этапах находятся случаи ДПР?</h2><span>Прямой этап формы</span></header><div className="preclaim-stages">{stages.map(stage=><div key={stage}><span>{stage}</span><i><b style={{width:`${rows.filter(x=>x.stage===stage).length/rows.length*100}%`}}/></i><strong>{rows.filter(x=>x.stage===stage).length}</strong></div>)}</div></article><aside className="preclaim-attention"><header><h2>Требует внимания</h2><span>Положительное отклонение и срок давности</span></header>{rows.filter(x=>x.deviation>0||x.limitationDays<180).slice(0,5).map(x=><article key={x.id}><strong>{x.id} · {x.contractor}</strong><span>Отклонение: +{x.deviation} дней</span><small>Срок давности: {x.limitationDays} дней</small></article>)}</aside></section><section className="preclaim-secondary"><article className="preclaim-card"><header><h2>Фактическая длительность ДПР</h2><span>Готовое количество дней</span></header><div className="preclaim-days">{rows.map(x=><div key={x.id}><span>{x.id}</span><i><b style={{width:`${x.days/Math.max(...rows.map(r=>r.days))*100}%`}}/></i><strong>{x.days}</strong></div>)}</div></article><article className="preclaim-card"><header><h2>Как распределяется сумма результата?</h2></header><div className="preclaim-money"><strong>{fmt(s.recognized)}</strong><span>признано, млн ₽</span><strong>{fmt(s.rejected)}</strong><span>отклонено, млн ₽</span></div></article></section><section className="pcm-note"><strong>Не рассчитывается:</strong> динамика закрытия — в форме нет единой прямой даты закрытия случая.</section></div>};
