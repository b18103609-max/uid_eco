import assert from'node:assert/strict';import test from'node:test';import{filterPreclaims,summarizePreclaims}from'../src/analytics/calculations/preclaim.ts';import{PRECLAIM_CASES}from'../src/analytics/data/preclaimData.ts';import{DEFAULT_ANALYTICS_FILTERS}from'../src/analytics/filters.ts';test('ДПР с отклонением считается по готовой положительной колонке',()=>{const s=summarizePreclaims(PRECLAIM_CASES);assert.equal(s.deviated,4);assert.equal(s.court,1)});test('ДПР поддерживает общий фильтр подрядчика',()=>assert.equal(filterPreclaims(PRECLAIM_CASES,{...DEFAULT_ANALYTICS_FILTERS,contractor:'БетаГрупп'}).length,1));
