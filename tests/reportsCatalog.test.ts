import assert from'node:assert/strict';import test from'node:test';import{ANALYTICS_NAV_PAGES,parseAnalyticsPath}from'../src/analytics/routing.ts';test('допсоглашения скрыты из навигации, но старая ссылка остаётся безопасной',()=>{assert.equal(ANALYTICS_NAV_PAGES.some(x=>x.id==='addendums'),false);assert.equal(parseAnalyticsPath('/uid_eco/tdk/analytics/addendums'),'addendums')});test('витрина отчётов остаётся в пользовательской навигации',()=>assert.equal(ANALYTICS_NAV_PAGES.some(x=>x.id==='reports'),true));
